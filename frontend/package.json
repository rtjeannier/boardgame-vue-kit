{
  "name": "frontend",
  "private": true,
  "version": "0.1.0",
  "description": "Vue 3 + Konva frontend for the boardgame-vue-kit template. Thin rendering layer — game logic lives in engine/.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "typecheck": "vue-tsc --noEmit",
    "story:dev": "histoire dev",
    "story:build": "histoire build",
    "story:preview": "histoire preview"
  },
  "dependencies": {
    "konva": "^9.3.18",
    "vue": "^3.5.0",
    "vue-konva": "^3.2.0"
  },
  "devDependencies": {
    "@histoire/plugin-vue": "^1.0.0-beta.1",
    "@vitejs/plugin-vue": "^6.0.4",
    "canvas": "^3.2.3",
    "histoire": "^1.0.0-beta.1",
    "typescript": "^5.7.3",
    "vite": "^7.1.0",
    "vue-tsc": "^2.2.0"
  }
}
