{
  "name": "drafting-example",
  "private": true,
  "version": "0.0.0",
  "description": "Sample consumer app: solo card-drafting mini-game using boardgame-vue-kit.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "typecheck": "vue-tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "boardgame-vue-kit": "*",
    "konva": "^9.3.18",
    "pinia": "^2.2.6",
    "vue": "^3.5.13",
    "vue-konva": "^3.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.4",
    "typescript": "^5.7.3",
    "vite": "^7.1.0",
    "vue-tsc": "^2.2.0"
  }
}
