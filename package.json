{
  "name": "boardgame-vue-kit-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "A Vue 3 + Tailwind board-game component kit. Monorepo hosting the kit package and example consumer apps.",
  "type": "module",
  "workspaces": [
    "kit",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w kit",
    "typecheck": "npm run typecheck -w kit && npm run typecheck -w examples/drafting",
    "story:dev": "npm run story:dev -w kit",
    "story:build": "npm run story:build -w kit",
    "example:dev": "npm run dev -w examples/drafting",
    "example:build": "npm run build -w examples/drafting"
  },
  "devDependencies": {
    "canvas": "^3.2.3"
  }
}
